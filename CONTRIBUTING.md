# 贡献指南

感谢你有兴趣贡献。本项目有一条贯穿始终的铁律，请先读它。

## 铁律：结论必须来自最终产物

这是本项目踩过坑之后定下的规矩：

> **任何"匹配 / 一致 / 达标"类结论，都必须在导出的成片上重算一遍。**
> 代理指标（源素材取样窗口、低分辨率代理片、缩略图目视）**只能用于选材阶段**，不能用于下结论。

我们曾犯过这个错：用源素材取样窗口量出的方向/速度，去推断成片的性质并据此写进报告，
直到在**成片**上复测才暴露。因此本仓库的所有案例报告都遵守这条规矩，
**不接受"看起来对"的结论**。

## 贡献技能

技能放在 `skills/<skill-name>/`，每个技能必须包含：

| 文件 | 必需 | 说明 |
|---|---|---|
| `SKILL.md` | 是 | 技能正文，含 YAML frontmatter |
| `test-prompts.json` | 是 | 触发/不触发的测试用例 |
| `test-results.md` | 是 | 验证记录，如实标注通过与否 |

### SKILL.md 规范

**frontmatter**（决定技能能否被 agent 正确加载）：

```yaml
---
name: your-skill-name          # 小写连字符，与目录名一致
description: |                 # 一句话说清"什么时候用 / 什么时候不用"
  用户在……时使用。也适用于……。
  不适用于：……。
---
```

**正文骨架**（R-I-C-E 四段，缺一不可）：

| 段 | 含义 | 要求 |
|---|---|---|
| `R — 原文 (Reading)` | 原始出处 | 带**出处与时间码**的短引用；不可大段照搬 |
| `I — 方法论骨架 (Interpretation)` | 结构化重构 | 把口语表述转成可判定的规则 |
| `C — 判定流程 (Criteria)` | 可执行步骤 | 每步都要能被检验，不能只给形容词 |
| `E — 边界与例外 (Edge cases)` | 反例与红线 | 明确"什么情况下这条不成立" |

### 硬性要求

- **不得照搬第三方原文大段内容**。引用需短、需带时间码，见 [ATTRIBUTION.md](ATTRIBUTION.md)。
- **不得包含未经验证的结论**。`test-results.md` 里的状态必须与实际情况一致，
  不允许出现"标了 ✓ 但实际没测"的情况。
- **描述要写"不适用于"**。一个没有边界的技能会误触发，比没有技能更糟。

## 贡献案例

案例放在 `cases/<序号>-<简短名>/`，需包含：

- `README.md` — 一句话结论 + 量化验收表（**数字必须在成片上实测**）
- 决策报告 — 选镜/参数怎么定的，为什么
- 验收报告 — 实测数据与工具命令
- 素材 — 仅限**你拥有权利**的素材。第三方素材请只留出处，不要上传文件

## 贡献工具

`tools/` 下的脚本必须：

- 参数化路径，**不得硬编码本机绝对路径**
- 在文件头 docstring 写明：用途、输入、输出、如何调用
- 输出可复现的数值（而不是只打印"通过"）

## 提交前自检

- [ ] 没有硬编码的本机路径（`C:\Users\...`、`D:\...`）
- [ ] 没有提交大文件（单文件 >50MB 会被拒；见 `.gitignore`）
- [ ] 没有提交无权利的第三方素材
- [ ] 结论类表述有对应的实测数据
- [ ] `test-results.md` / 验收报告的状态与实际一致

## 流程

1. Fork 本仓库
2. 开分支：`git checkout -b feat/your-skill`
3. 提交：说明**为什么**要这个改动，而不只是改了什么
4. 发起 Pull Request，描述里附上你的验证方式与结果

## 许可

提交即表示你同意你的贡献按本仓库的双许可发布
（代码 [MIT](LICENSE) / 内容 [CC BY 4.0](LICENSE-CONTENT.md)）。